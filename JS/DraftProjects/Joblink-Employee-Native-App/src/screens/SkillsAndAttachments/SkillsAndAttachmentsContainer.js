import {connect} from "react-redux";
import SkillsAndAttachments from "./SkillsAndAttachments";

// const mapStateToProps = (state) => ({})

const SkillsAndAttachmentsContainer = connect(null, null)(SkillsAndAttachments);

export default SkillsAndAttachmentsContainer;
