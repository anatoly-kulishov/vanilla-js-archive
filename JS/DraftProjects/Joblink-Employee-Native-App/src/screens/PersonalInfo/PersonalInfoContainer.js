import {connect} from "react-redux";
import PersonalInfo from "./PersonalInfo";

// const mapStateToProps = (state) => ({})

const PersonalInfoContainer = connect(null, null)(PersonalInfo);

export default PersonalInfoContainer;
