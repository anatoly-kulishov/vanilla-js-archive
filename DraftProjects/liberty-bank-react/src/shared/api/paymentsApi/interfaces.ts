export interface ICREATE_SELECTED {
  clientId: string;
  providerId: string;
  providerName: string;
  paymentType: string;
  indificationNumber: string;
}

export interface ICREATE_SELECTED_RESPONSE {
  message: string;
}
