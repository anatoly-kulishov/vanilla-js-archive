import { IconGallery, IconItem, Meta } from '@storybook/blocks';
import { Icon, SVGIcons } from "..";
import style from './styles.module.scss'

<Meta title='Assets/icons' />

<IconGallery>
  {Object.keys(SVGIcons)
    .map(iconName => (
      <IconItem name={iconName}>
        <div className={style.icon}>
          <Icon icon={iconName} width={'100%'} height={'100%'} />
        </div>
      </IconItem>
    ))}
</IconGallery>
