import { IconGallery, IconItem, Meta } from '@storybook/blocks';
import { Image, SVGImages } from "..";
import styles from './styles.module.scss'

<Meta title='Assets/images' />

<IconGallery>
  {Object.keys(SVGImages).map(imageName => (
    <IconItem name={imageName}>
      <div className={styles.image}>
        <Image image={imageName} widthAndHeight={'100%'} />
      </div>
    </IconItem>
  ))}
</IconGallery>
