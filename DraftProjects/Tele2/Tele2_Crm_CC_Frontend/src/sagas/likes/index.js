// import { all, takeEvery } from 'redux-saga/effects'

// Balance
// import { LIKES_FETCH, LIKE_CREATE } from 'reducers/likes/likesReducer'
// import { fetchLikesSaga, createLikeSaga } from './likesSaga'

export default function * () {
  // yield all([takeEvery(LIKES_FETCH, fetchLikesSaga), takeEvery(LIKE_CREATE, createLikeSaga)])
}
