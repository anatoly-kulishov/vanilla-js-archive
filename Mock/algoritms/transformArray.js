/**
 * Задача: "Трансформация массива"
 *
 * Напишите функцию transformArray, которая принимает массив чисел и набор функций-преобразователей,
 * и возвращает новый массив, в котором каждый элемент является результатом применения
 * всех функций-преобразователей к соответствующему элементу исходного массива.
 *
 * Функция должна работать следующим образом:
 * 1. Принимать первым аргументом массив чисел
 * 2. Принимать последующими аргументами функции-преобразователи
 * 3. Возвращать новый массив, не изменяя исходный
 * 4. Если передано меньше двух аргументов, выбрасывать ошибку
 *
 * Пример функций-преобразователей:
 * - double - удваивает число
 * - square - возводит число в квадрат
 * - addOne - прибавляет к числу единицу
 */

function transformArray(arr, ...transformers) {
    // Ваш код здесь
}

// Пример использования:
const numbers = [1, 2, 3, 4, 5];

const double = (x) => x * 2;
const square = (x) => x * x;
const addOne = (x) => x + 1;

console.log(transformArray(numbers, double, square));
// Должно вывести: [4, 16, 36, 64, 100]

console.log(transformArray(numbers, addOne, double, square));
// Должно вывести: [8, 18, 32, 50, 72]

console.log(transformArray(numbers));
// Должно выбросить ошибку: "Необходимо передать как минимум один преобразователь"

console.log(numbers);
// Должно вывести: [1, 2, 3, 4, 5] (исходный массив не изменился)
