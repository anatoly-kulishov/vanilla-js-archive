/**
 * const a = 1; //  Cannot access 'a' before initialization
 * let b = 2; //  Cannot access 'b' before initialization
 * var c = 3; // undefined
 */
console.log(a, b, c); // ? ? ?

const a = 1;
let b = 2;
var c = 3;
