/**
 *  return str.split(" ").reduce((acc, curr) => {
 *     return `${acc} ${curr.slice(0, 1).toUpperCase()}${curr.slice(1)}`;
 *  }, "");
 */

/** Функция трансформации букв в заглавный регистр */

const toCapitalCase = (str) => {};

const str = "Lorem ipsum is simply";
console.log(toCapitalCase(str));   // "Lorem Ipsum Is Simply"
